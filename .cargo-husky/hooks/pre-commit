#!/bin/sh

# Auto-format Rust files and stage changes.
echo "Running cargo fmt..."
cargo fmt

# Stage any formatting changes.
git diff --name-only --cached --diff-filter=ACM | grep '\.rs$' | xargs -r git add

echo "✓ Rust files formatted"
